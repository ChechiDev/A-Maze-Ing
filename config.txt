# A-Maze-ing default configuration.
# Use one KEY=VALUE pair per non-comment line.
# Lines starting with # are comments.
# Inline comments are not supported in KEY=VALUE lines.
# ENTRY and EXIT use public x,y coordinates.
# Required keys: WIDTH, HEIGHT, ENTRY, EXIT, OUTPUT_FILE, PERFECT.
# Optional keys supported initially: SEED.
WIDTH=22
HEIGHT=22
ENTRY=2,3
EXIT=8,8
OUTPUT_FILE=maze.txt
PERFECT=True
SEED=42
